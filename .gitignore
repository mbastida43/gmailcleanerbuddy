# Dependências
node_modules/

# Segredos e ambiente — NUNCA versionar
.env
.env.*
!.env.example
*.pem
*.key

# Logs
npm-debug.log*
*.log

# SO / editores
.DS_Store
Thumbs.db
.vscode/
.idea/

# Arquivos históricos
*.old

# Anotações de trabalho — temporárias, só fazem sentido na máquina local
/estado_atual.txt

# Skills locais do Claude Code — ferramenta pessoal, não faz parte do app
/.claude/skills/

# Build TypeScript (gerado a partir de src/)
/dist/
/public/app.js
/public/app.js.map

# Empacotamento Android (TWA/Bubblewrap) — gerado, e a chave de assinatura
# NUNCA pode ser versionada: quem tem o keystore publica updates no seu nome.
/android/
*.keystore
*.jks
*.aab
*.apk
